# UI/UX

Use the project's design system, component contracts, and styling conventions.
Preserve semantics, keyboard behavior, focus, and accessible names. Cover loading,
empty, error, pending, and destructive states when applicable. Avoid redundant
fallbacks and meaningless default text. Verify relevant viewport sizes and interactions;
record actual visual/manual evidence or explicitly identify what is unavailable.
